# 国内镜像配置（中国大陆网络环境必需）
#
# 为什么需要？
# 1. registry 走 npmmirror，装包速度从「几分钟」降到「十几秒」
# 2. better-sqlite3 这类原生模块，npm 会去 GitHub Releases 下预编译二进制；
#    国内常常拉不动，于是回落到「本地编译」，然后报错：
#        gyp ERR! find VS You need to install the latest version of Visual Studio
#    这一行配置让它改从 npmmirror 的二进制镜像下载，直接免掉编译。
#
# 如果换到海外网络环境，可以删掉这个文件（或注释掉这几行）。
registry=https://registry.npmmirror.com
better-sqlite3_binary_host_mirror=https://registry.npmmirror.com/-/binary/better-sqlite3
